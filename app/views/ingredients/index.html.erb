<h1>Składniki</h1>
<%= link_to 'Nowy Składnik', new_ingredient_path %>
<br>
<br>
<table style="width:100%">
  <tr>
    <td></td>
    <td>Szczegóły</td>
    <td></td>
  </tr>
  <% @ingredients.each do |i| %>
  <tr>
    <td><%= i.name %></td>
    <td><%= link_to 'Pokaż', i %></td>
    <td><%= link_to 'Usuń', i, :confirm => 'Usunąć?', :method => :delete %></td>
  </tr>
  <% end %>
</table> 

<br>
